# Astro 移行ステータス管理 (ASTRO_MIGRATION_STATUS.md)

本ドキュメントは、UcyuhouプロジェクトにおけるAstroへの構造移行の進捗状況をリアルタイムで追跡するための管理表である。

---

## 1. 現在のフェーズ
- **現在のフェーズ**: `Phase 5：トップ・固定ページ・検索移行`
- **ステータス**: `完了・ユーザー確認済み`

> [!NOTE]
> トップページ本移植、固定4ページ（About, Contact, Privacy, Terms）の移行、Header/Footer正式リンクの有効化、Content Collections全26記事を対象とする全文検索、search-index.jsonのビルド自動生成、日本語IME入力制御、検索モーダルの段階展開と上端固定の実装が完了し、ユーザー様によるSafari実画面確認をパスしました。
> 次回より新規エージェントでPhase 6（表示崩れ検証と静的ビルド - 最終監査）を開始します。

---

## 2. フェーズ別進捗

| フェーズ | 作業内容 | ステータス | 完了基準 |
| :--- | :--- | :--- | :--- |
| **Phase 0** | 移行設計書(SPEC, STATUS, CONTRACT, MAP)の作成・再調整 | **完了** | 4つの設計ドキュメントの修正およびユーザーによる承認が得られること。 |
| **Phase 1** | Astro基盤作成、package/config配置、CSS・アセットのコピー | **完了** | Astroプロジェクト初期化が完了し、`pnpm dev` でCSSや画像アセットがエラーなく参照されること。既存HTML/CSS等は削除・移動せず残す。 |
| **Phase 2** | レイアウト・共通コンポーネント化 | **完了** | ヘッダー、フッター、カード、フォームがAstroコンポーネントとして動作すること。 |
| **Phase 3** | 代表記事2本の移行とContent Collections定義 | **完了** | `src/content.config.ts` 定義完了。通常記事（satori）と宇宙通信（tsushin）の代表各1本のみがMarkdownとしてパース可能なこと。 |
| **Phase 4** | 動的ルートの実装・検証、残り24記事の一括移行 | **完了** | 全26記事の表示・ダークテーマ切り替え・カテゴリールートおよび受信履歴が正常に動作すること。 |
| **Phase 5** | 静的・固定ページの移行、検索インデックスのビルド統合 | **完了** | `pnpm build` 内で自動的に検索インデックスが再生成されること。個別の手動実行を廃止。 |
| **Phase 6** | 表示崩れ検証と静的ビルド (最終監査) | **未着手** | 代表ページ（トップ、通常、宇宙通信、カテゴリ、固定）の1440px/390px幅において、ユーザーが認識できる重大なデザイン差がないこと。 |

---

## 3. まだ実装していないこと（現在の保留事項）

- **Phase 6** (最終監査・表示崩れ検証) は未着手です。

---

## 4. 次の予定

**新規エージェントでPhase 6（表示崩れ検証と静的ビルド - 最終監査）を開始する。**

> [!IMPORTANT]
> - 正式トップページ移行完了
> - 固定4ページ移行完了
> - Header / Footer正式リンク有効化完了
> - 全26記事の全文検索実装完了
> - search-index.jsonのビルド自動生成完了
> - 日本語IME入力制御確認完了
> - 検索モーダルの段階展開と上端固定確認完了
> - Safariで通常幅および390px幅の表示確認完了
> - 既存の旧ファイル（HTML, CSS, JS, 画像）には一切触れておらず、完全に元の状態を維持しています。

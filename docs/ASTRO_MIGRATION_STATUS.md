# Astro 移行ステータス管理 (ASTRO_MIGRATION_STATUS.md)

本ドキュメントは、UcyuhouプロジェクトにおけるAstroへの構造移行の進捗状況をリアルタイムで追跡するための管理表である。

---

## 1. 現在のフェーズ
- **現在のフェーズ**: `Phase 3：代表記事2本の移行とContent Collections定義`
- **ステータス**: `完了`

> [!NOTE]
> Safariを用いて、1440px（デスクトップ幅）および390px（モバイル幅）での実画面確認が完了しました。
> 横方向のはみ出し、要素の重なり、ヘッダー/3記事カード/フッター/ニュースレターの表示崩れが一切なく、未実装のページへ遷移しない（安全なリンク無効化）ことを確認済みです。

---

## 2. フェーズ別進捗

| フェーズ | 作業内容 | ステータス | 完了基準 |
| :--- | :--- | :--- | :--- |
| **Phase 0** | 移行設計書(SPEC, STATUS, CONTRACT, MAP)の作成・再調整 | **完了** | 4つの設計ドキュメントの修正およびユーザーによる承認が得られること。 |
| **Phase 1** | Astro基盤作成、package/config配置、CSS・アセットのコピー | **完了** | Astroプロジェクト初期化が完了し、`pnpm dev` でCSSや画像アセットがエラーなく参照されること。既存HTML/CSS等は削除・移動せず残す。 |
| **Phase 2** | レイアウト・共通コンポーネント化 | **完了** | ヘッダー、フッター、カード、フォームがAstroコンポーネントとして動作すること。 |
| **Phase 3** | 代表記事2本の移行とContent Collections定義 | **完了** | `src/content.config.ts` 定義完了。通常記事（satori）と宇宙通信（tsushin）の代表各1本のみがMarkdownとしてパース可能なこと。 |
| **Phase 4** | 動的ルートの実装・検証、残り24記事の一括移行 | **未着手** | 代表記事2本の表示・ダークテーマ切り替えが検証でき、問題なければ残りの24記事を一括してMarkdownへ移行完了すること。 |
| **Phase 5** | 静的・固定ページの移行、検索インデックスのビルド統合 | **未着手** | `pnpm build` 内で自動的に検索インデックスが再生成されること。個別の手動実行を廃止。 |
| **Phase 6** | 表示崩れ検証と静的ビルド (最終監査) | **未着手** | 代表ページ（トップ、通常、宇宙通信、カテゴリ、固定）の1440px/390px幅において、ユーザーが認識できる重大なデザイン差がないこと。 |

---

## 3. まだ実装していないこと（現在の保留事項）

- **Astro基盤作成**: Astro基盤と依存関係のインストールは完了済みです。
- **設定ファイル**: `package.json`、`astro.config.mjs`、`tsconfig.json`、`pnpm-lock.yaml`、`pnpm-workspace.yaml` は作成済みです。
- **CSSとアセット**: 既存CSSのAstro側へのコピー、および既存画像の `public/` 側へのコピーは完了済みです。
- **既存の旧ファイル保護**: 既存の旧HTML、元のCSS、元のJS、元の画像は未変更で元の位置に完全に残っています。
- **移行フェーズ**: 代表記事2本（satori, tsushin）のContent Collections移行は完了しました。動的記事ページやカテゴリページは未実装（未着手）です。残り24記事もまだ移行していません。

---

## 4. 次の予定

**次は新規エージェントで Phase 4：記事・カテゴリー動的ルートの実装と残り記事移行を開始する。**

> [!IMPORTANT]
> - Phase 3は**完了**しました。
> - 代表記事2本だけを移行し、Content Collectionsスキーマ（src/content.config.ts）の定義を行いました。
> - 動的記事ルート（src/pages/articles/[slug].astro）やカテゴリページ等は未実装であり、残り24記事も未移行のまま残されています。
> - 次のPhase 4は、別の新規エージェントがセッションを引き継いで担当します。
> - 既存のHTML、CSS、JavaScript、画像には一切触れておらず、完全に元の状態を維持しています。
